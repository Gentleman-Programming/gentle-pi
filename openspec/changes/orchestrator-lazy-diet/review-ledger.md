# Review Ledger — orchestrator-lazy-diet

Round 1 — judgment-day (jd-judge-a + jd-judge-b, blind parallel) over the DESIGN artifacts. Pre-implementation. Verified sound by both: getOrchestratorPrompt cache/substitution description, :123-133 citation, 22,626 B total, no lazy-file double-delivery via the cache.

| id | lens | severity | file:line | status | summary |
|----|------|----------|-----------|--------|---------|
| JD-001 | judgment-day | CRITICAL | design.md:7-26 (Inventory "(measured)") | fixed | Inventory table replaced with the judges' `wc -c`-verified figures (self-recomputed with `awk 'NR>=a&&NR<=b'\|wc -c` per section, reconciles to 22,626 exact), labeled "(measured, wc -c)". |
| JD-002 | judgment-day | CRITICAL | design.md:19,45-47,77-81 | fixed | Rebuilt core budget from scratch: drafted + measured (`wc -c`) every condensed core row incl. Delegation (1,344B), Memory (785B), 4R (788B); pointer-sentence bytes are embedded per-row, not a separate add-on. Honest total = 9,826B (13 rows 9,071 + 755 reserved), over the old 8,192B target — threshold revised to ≤10,240B with rationale (see Decision "Core budget rebuilt from measured drafts"). |
| JD-003 | judgment-day | CRITICAL (coordination) | design.md:14,16,45-47 (vs persona-single-channel/design.md:135-139,180-199) | fixed | Identity (149B) and Language LB1 pointer reuse persona-single-channel's delivered pointer text VERBATIM, near-zero core cost. Decision rationale corrected: the budget benefit comes from core OMITTING identity/persona/language bytes entirely (pointing to the wrapper instead), not from a wrapper shrink (wrapper actually grows +0.4KB per persona's design). |
| JD-004 | judgment-day | CRITICAL (coordination) | design.md:98-101 (vs port-review-ledger-contract/design.md:99) | fixed | Added "Reserved: incoming Review Execution Contract" inventory row with a bounded estimate (~755B, from that change's design.md:117-126 persistence-branches prose) included in the Core Budget total, plus a hard commitment to re-verify the budget with `wc -c` against the rebased file before the fixture freeze. |
| JD-005 | judgment-day | WARNING | design.md:87 (vs gentle-ai.ts:51-52,123-133; spec Requirement 1) | fixed | Testing Strategy byte-budget row now targets `getOrchestratorPrompt()`'s RETURN value with a stubbed `ASSETS_DIR`/cwd, not the raw file with unresolved placeholders. |
| JD-006 | judgment-day | WARNING | design.md:73,90 (vs tests/gentle-ai.test.ts:40-64) | fixed | Testing Strategy regression row now states explicitly that `:40` is a 3-file uniform loop and only the `orchestrator.md` iteration is repointed to the core+delegation-ref union; README/SKILL.md iterations are unchanged. |
| JD-007 | judgment-day | WARNING | design.md:88 (Testing Strategy) | fixed | Added a Core-alone load-bearing assertions row: "4-file rule", "400 changed lines", the 6 named trigger labels, and the 4 lens names must be assertable in core alone, plus disposition-mapped per-rule assertions replacing the blanket union-includes sweep. |
| JD-008 | judgment-day | WARNING (theoretical) | design.md:36,79 (vs orchestrator.md:34,36) | fixed | LB3/LB4 (artifact/comment language) now kept VERBATIM in core (not moved to lazy) — Inventory + Core Budget rows updated accordingly (+~1.1KB honest cost, drives the budget revision in JD-002). |
| JD-009 | judgment-day | WARNING | design.md:98-101 (cross-design) | fixed | Added a corroboration note citing `port-review-ledger-contract/design.md:202-207` (Sequencing / Coordination — hard merge order AFTER persona-single-channel, BEFORE orchestrator-lazy-diet), closed by that change's own design content. |
| JD-010 | judgment-day | SUGGESTION | design.md:30-33,46,90 | fixed | Dropped the uncorroborated "gentle-ai's proven 7.8KB core" comparator (no citation found in the gentle-ai repo); added the lazy-copy byte-identical/no-reflow constraint note to the Verbatim-to-lazy decision; fixed "gente-ai.test.ts" → "tests/gentle-ai.test.ts" in the Testing Strategy table. |
