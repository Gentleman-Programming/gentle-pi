# Review Ledger — persona-single-channel

Round 1 — judgment-day (jd-judge-a + jd-judge-b, blind parallel) over the DESIGN artifacts. Pre-implementation. Both judges independently verified: the orchestrator-side union sweep is COMPLETE (no content loss) and the LB2-LB5-are-unique judgment is substantively correct.

| id | lens | severity | file:line | status | summary |
|----|------|----------|-----------|--------|---------|
| JD-001 | judgment-day | CRITICAL | design.md:88,96 (vs gentle-ai.ts:158,186) | fixed | Dropped the design.md:96 post-`${languageBoundary}` line entirely; trimmed the wrapper Identity contract bullet to "Follow the currently selected persona mode." (language-match clause removed to avoid re-duplication); folded one language-match clause into `GENTLEMAN_PERSONA_PROMPT` (mirrors NEUTRAL:158, verified GENTLEMAN previously had no language-match bullet); NEUTRAL's :158 line kept as its single, unchanged copy; extended the Testing Strategy duplication guard with a concept-level regex check asserting the language-match concept appears exactly once per rendered mode. |
| JD-002 | judgment-day | CRITICAL | design.md:29,135-139 | fixed | Replaced the byte table with judge-measured figures (re-verified via `bat`+`wc -c`: LB1 line30 = 262B exact match, :28-42 = 2,117B exact match, wrapper Identity block :179-184 = 438B exact match); retitled section "Byte estimates (judge-verified where noted; authoritative wc -c lands in the apply RED test)"; net gentleman now stated as ≈ −0.2KB. |
| JD-003 | judgment-day | WARNING (apply-blocking) | specs/session-persona/spec.md:11 (vs design.md:110,210) | fixed | Amended spec.md:11 requirement text: Identity Contract MUST reduce to a pointer (unchanged); Language Boundary's duplicated LB1 portion MUST reduce to a pointer while LB2-LB5 (unique) are retained verbatim. Also split the "Pointer preserves discoverability" scenario into two scenarios (Identity-only full pointer; Language Boundary partial pointer + retained uniques) and updated the Acceptance Criteria bullet to match. Closed design.md's Open Questions bullet 3 as resolved ([x]). |
| JD-004 | judgment-day | WARNING | design.md:172 (vs gentle-ai.ts:51-52,118-133) | fixed | Corrected the Testing Strategy reading-mechanism citation: `getOrchestratorPrompt()` resolves `assets/orchestrator.md` via `PACKAGE_ROOT = dirname(dirname(fileURLToPath(import.meta.url)))` (gentle-ai.ts:51-52), NOT `process.cwd()`; added the module-level cache note (`orchestratorPromptCache`, gentle-ai.ts:125-134 — first read wins per process, relevant to mutation-based test fixtures). |
| JD-005 | judgment-day | WARNING | design.md:56 vs :90 | fixed | Table A memory row verdict corrected from "MERGE: orchestrator phrasing + wrapper's never-invent clause" to "KEEP (wrapper)": wrapper's :183 phrasing already carries the never-invent clause; orchestrator's :19 phrasing is superseded, not merged — now consistent with the final post-change text. |
| JD-006 | judgment-day | WARNING (methodological) | design.md:47-58 (Table A) | fixed | Added explicit Table A rows for wrapper :180 and :181 (each disposition: MERGE into self-description block, subsumed by the richer translated paragraph); added a named per-rule Testing Strategy assertion for wrapper :180/:181 in the line-level union sweep row. |
| JD-007 | judgment-day | SUGGESTION | design.md:22,52,168,191-196; gentle-ai.ts:198 | fixed | Clarified :177/orchestrator:7 are byte-identical (not a paraphrase) while the surrounding block is; replaced the undefined "W1" shorthand with explicit line refs (:180-181); noted in the Testing Strategy header that `pnpm test` also runs `test:harness` (package.json:38); added a derivation showing the diet's ~1KB core-budget benefit (avoided Identity + Language Boundary in-core paraphrases + LB1 dup removal ≈ 900B-1.0KB) and reconciled the :28-42 (2,117B, judge-verified) vs the diet's :28-43 (2,118B, re-measured — diet's own 2,350B estimate was ≈11% high); documented the third memory-rule copy at gentle-ai.ts:198 as a known out-of-scope residual (Table A footnote). |
