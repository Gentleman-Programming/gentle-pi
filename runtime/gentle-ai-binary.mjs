// Generated by scripts/build-git-commit-transaction-runner.mjs. Do not edit.
import { createHash } from "node:crypto";
import { existsSync, lstatSync, readFileSync, readdirSync } from "node:fs";
import { dirname, isAbsolute, join, relative, resolve, sep } from "node:path";
import {
	GENTLE_AI_INSTALL_METHOD,
	GENTLE_AI_WINDOWS_SOURCE_MODULE,
	GENTLE_AI_WINDOWS_SOURCE_MODULE_CHECKSUM,
	GENTLE_AI_WINDOWS_SOURCE_PACKAGE_PATH,
	GENTLE_AI_WINDOWS_SOURCE_TAG,
	INSTALLER_VERSION,
	isGentleAiWindowsGoVersionSupported,
	isWindowsGoSumdbSourceTarget,
	resolveGentleAiAssetsArchive,
	resolveGentleAiReleaseAsset,
} from "../scripts/gentle-ai-installer.mjs";
import {
	RELEASE_ARTIFACT_MANIFEST_FILE_NAME,
	assertExactMemberSet,
	decodeArtifactManifest,
	treeDigest,


} from "./release-artifact.mjs";
import { fileURLToPath } from "node:url";

export const GENTLE_AI_BINARY_MISSING_CODE = "package-local-binary-missing";
// Derived from the one authoritative pinned version in
// scripts/gentle-ai-installer.mjs rather than repeating the literal here, so
// the two can never independently drift apart the way they once did.
export const GENTLE_AI_VERSION = INSTALLER_VERSION;

export class PackageLocalGentleAiBinaryMissingError extends Error {
	         code = GENTLE_AI_BINARY_MISSING_CODE;
	constructor(path        ) {
		super(
			`${GENTLE_AI_BINARY_MISSING_CODE}: Gentle AI v${GENTLE_AI_VERSION} is not installed at ${path}. Reinstall gentle-pi, or use GENTLE_PI_SKIP_GENTLE_AI_INSTALL=1 only for development/offline installs.`,
		);
		this.name = "PackageLocalGentleAiBinaryMissingError";
	}
}

export function gentleAiBinaryPath(
	packageRoot = dirname(dirname(fileURLToPath(import.meta.url))),
	platform = process.platform,
)         {
	return join(
		resolve(packageRoot),
		".gentle-ai",
		`v${GENTLE_AI_VERSION}`,
		platform === "win32" ? "gentle-ai.exe" : "gentle-ai",
	);
}

function sha256(value        )         {
	return createHash("sha256").update(value).digest("hex");
}

function isConfined(path        , directory        )          {
	const relativePath = relative(directory, path);
	return relativePath !== "" && !relativePath.startsWith("..") && !isAbsolute(relativePath);
}

function assertRegularNonSymlink(path        )       {
	const details = lstatSync(path);
	if (!details.isFile() || details.isSymbolicLink()) throw new Error("expected regular non-symlink file");
}

function assertPosixExecutable(path        , platform        )       {
	if (platform !== "win32" && (lstatSync(path).mode & 0o111) === 0) {
		throw new Error("expected executable POSIX binary");
	}
}









// Grows the manifest key set with the assets provenance (design D3/D4)
// without weakening `isCanonicalManifest`'s exact-key-count/string-equality
// discipline below: a missing or forged assets field fails resolution
// exactly like a missing or forged binary field always has. Mirrors
// scripts/gentle-ai-installer.mjs's `assetsManifestFields` byte-for-byte so
// the writer (installer) and reader (this module) always agree on shape.
function assetsManifestFields(assetsArchive                         )                                  {
	return {
		assetsAsset: assetsArchive.name,
		assetsArchiveSha256: assetsArchive.sha256,
		assetsTreeSha256: assetsArchive.treeSha256,
		contractMajor: assetsArchive.contractMajor,
		layoutVersion: assetsArchive.layoutVersion,
	};
}

function signedReleaseManifest(asset                                                        , assetsArchive                         )                                  {
	return { version: GENTLE_AI_VERSION, asset: asset.name, assetSha256: asset.sha256, binarySha256: asset.binarySha256, ...assetsManifestFields(assetsArchive) };
}

function windowsSourceManifest(manifest                         , binarySha256        , platform        , assetsArchive                         )                                  {
	if (!isWindowsGoSumdbSourceTarget(platform, process.arch)) throw new Error("unsupported Windows source architecture");
	const architecture = process.arch === "x64" ? "x64" : "arm64";
	const goVersion = manifest.goVersion;
	if (manifest.moduleChecksum !== GENTLE_AI_WINDOWS_SOURCE_MODULE_CHECKSUM || typeof goVersion !== "string" || !isGentleAiWindowsGoVersionSupported(goVersion)) throw new Error("invalid Windows source provenance");
	return {
		version: GENTLE_AI_VERSION,
		method: GENTLE_AI_INSTALL_METHOD.GO_SUMDB_SOURCE_BUILD,
		package: GENTLE_AI_WINDOWS_SOURCE_PACKAGE_PATH,
		module: GENTLE_AI_WINDOWS_SOURCE_MODULE,
		tag: GENTLE_AI_WINDOWS_SOURCE_TAG,
		architecture,
		binarySha256,
		moduleChecksum: GENTLE_AI_WINDOWS_SOURCE_MODULE_CHECKSUM,
		goVersion,
		goos: "windows",
		goarch: process.arch === "x64" ? "amd64" : "arm64",
		buildMode: "exe",
		compiler: "gc",
		cgoEnabled: "0",
		...assetsManifestFields(assetsArchive),
	};
}

function expectedRuntimeManifest(platform        , binarySha256        , manifest                         , assetsArchive                         )                                  {
	if (platform === "win32") return windowsSourceManifest(manifest, binarySha256, platform, assetsArchive);
	const asset = resolveGentleAiReleaseAsset(platform, process.arch);
	if (binarySha256 !== asset.binarySha256) throw new Error("runtime binary does not match pinned release digest");
	return signedReleaseManifest(asset, assetsArchive);
}

function isCanonicalManifest(contents        , manifest                         , expected                                 )          {
	return contents === `${JSON.stringify(expected)}\n`
		&& Object.keys(manifest).length === Object.keys(expected).length
		&& Object.entries(expected).every(([key, value]) => manifest[key] === value);
}

function sameFile(before                              , after                              )          {
	return before.dev === after.dev && before.ino === after.ino && before.size === after.size && before.mtimeMs === after.mtimeMs;
}

export function resolveGentleAiBinary(
	packageRoot = dirname(dirname(fileURLToPath(import.meta.url))),
	platform = process.platform,
	readBinary                           = readFileSync,
)         {
	const binaryPath = gentleAiBinaryPath(packageRoot, platform);
	const versionDirectory = dirname(binaryPath);
	const manifestPath = join(versionDirectory, "integrity.json");
	try {
		if (!isAbsolute(binaryPath) || !isConfined(binaryPath, versionDirectory)) throw new Error("unconfined binary");
		for (const path of [join(resolve(packageRoot), ".gentle-ai"), versionDirectory]) {
			const details = lstatSync(path);
			if (!details.isDirectory() || details.isSymbolicLink()) throw new Error("symlinked runtime directory");
		}
		assertRegularNonSymlink(binaryPath);
		assertPosixExecutable(binaryPath, platform);
		assertRegularNonSymlink(manifestPath);
		const beforeBinary = lstatSync(binaryPath);
		const beforeManifest = lstatSync(manifestPath);
		const manifestContents = readFileSync(manifestPath, "utf8");
		const manifest = JSON.parse(manifestContents)                           ;
		const binarySha256 = sha256(readBinary(binaryPath));
		// The hot path stays one file hash (design D4): only the pinned
		// scalar assets identity is read here (a single small JSON parse),
		// never the assets tree itself — a missing or forged assets digest
		// still fails resolution because it fails the canonical manifest
		// string-equality check below, exactly like a forged binary digest.
		const assetsArchive = resolveGentleAiAssetsArchive(resolve(packageRoot));
		const expected = expectedRuntimeManifest(platform, binarySha256, manifest, assetsArchive);
		if (!isCanonicalManifest(manifestContents, manifest, expected)) throw new Error("invalid runtime integrity manifest");
		const afterBinary = lstatSync(binaryPath);
		const afterManifest = lstatSync(manifestPath);
		if (!sameFile(beforeBinary, afterBinary) || !sameFile(beforeManifest, afterManifest)) throw new Error("runtime replaced during verification");
		assertPosixExecutable(binaryPath, platform);
		return binaryPath;
	} catch {
		throw new PackageLocalGentleAiBinaryMissingError(binaryPath);
	}
}

export const GENTLE_AI_ASSETS_MISSING_CODE = "package-local-assets-missing";

export class PackageLocalGentleAiAssetsMissingError extends Error {
	         code = GENTLE_AI_ASSETS_MISSING_CODE;
	constructor(path        ) {
		super(
			`${GENTLE_AI_ASSETS_MISSING_CODE}: the verified Gentle AI v${GENTLE_AI_VERSION} assets bundle is not installed at ${path}. Reinstall gentle-pi, or use GENTLE_PI_SKIP_GENTLE_AI_INSTALL=1 only for development/offline installs.`,
		);
		this.name = "PackageLocalGentleAiAssetsMissingError";
	}
}

export function gentleAiAssetsDirectoryPath(
	packageRoot = dirname(dirname(fileURLToPath(import.meta.url))),
	platform = process.platform,
)         {
	return join(dirname(gentleAiBinaryPath(packageRoot, platform)), "assets");
}






// Lists every filesystem entry under `directory` using `lstat` (never
// following a symlink into a different tree), returning files and symlinks
// alike as leaf "members" — directories are recursed into, never reported.
// This is deliberately dumb about *type*: a symlink masquerading as an
// expected path is still reported here (so the exact-set check below cannot
// miss it by silently skipping it); type/mode rejection is a separate,
// later per-file check, matching design D4's ordering.
function listAssetMembers(directory        , base        , found                     = [])                     {
	for (const entry of readdirSync(directory, { withFileTypes: true })) {
		const path = join(directory, entry.name);
		const details = lstatSync(path);
		if (details.isDirectory() && !details.isSymbolicLink()) {
			listAssetMembers(path, base, found);
			continue;
		}
		found.push({ path: relative(base, path).split(sep).join("/"), size: details.size });
	}
	return found;
}

// design D4: `resolveGentleAiAssets` does the N-file work, lazily, only for
// snapshot readers. Order (constraint carried from design/tasks 3.8):
//   1. exact path-set equality against the manifest entries FIRST — a tree
//      digest over "files I found" cannot detect an added file, because a
//      digest walk only ever revisits declared entries;
//   2. THEN per-file lstat/confinement/mode checks;
//   3. THEN a whole-set `sameFile` TOCTOU re-check across every entry file
//      plus the manifest, not only the first and last file.
export function resolveGentleAiAssets(
	packageRoot = dirname(dirname(fileURLToPath(import.meta.url))),
	platform = process.platform,
	readEntryFile                           = readFileSync,
)                                                          {
	const assetsDirectory = gentleAiAssetsDirectoryPath(packageRoot, platform);
	const versionDirectory = dirname(assetsDirectory);
	try {
		if (!isConfined(assetsDirectory, versionDirectory)) throw new Error("unconfined assets directory");
		const assetsDetails = lstatSync(assetsDirectory);
		if (!assetsDetails.isDirectory() || assetsDetails.isSymbolicLink()) throw new Error("assets directory is not a real directory");

		const manifestPath = join(assetsDirectory, RELEASE_ARTIFACT_MANIFEST_FILE_NAME);
		if (!isConfined(manifestPath, assetsDirectory)) throw new Error("unconfined assets manifest");
		assertRegularNonSymlink(manifestPath);
		const beforeManifest = lstatSync(manifestPath);
		const manifest = decodeArtifactManifest(readFileSync(manifestPath));

		// stage: exact path-set equality (must run before per-file checks).
		const found                  = listAssetMembers(assetsDirectory, assetsDirectory).map((member) => ({ ...member, typeChar: "-" }));
		assertExactMemberSet(found, RELEASE_ARTIFACT_MANIFEST_FILE_NAME, manifest.entries);

		// per-file lstat/confinement/mode/digest checks, snapshotting each
		// entry's "before" state right before validating it.
		const beforeEntries                                                                                   = [];
		for (const entry of manifest.entries) {
			const entryPath = join(assetsDirectory, entry.path);
			if (!isConfined(entryPath, assetsDirectory)) throw new Error(`unconfined asset entry: ${entry.path}`);
			const before = lstatSync(entryPath);
			beforeEntries.push({ path: entry.path, entryPath, before });
			assertRegularNonSymlink(entryPath);
			if (before.size !== entry.size) throw new Error(`asset ${entry.path} extracted size disagrees with the manifest size`);
			if ((before.mode & 0o111) !== 0) throw new Error(`asset ${entry.path} is executable; only mode 0644 is admitted`);
			const digest = `sha256:${sha256(readEntryFile(entryPath))}`;
			if (digest !== entry.digest) throw new Error(`asset ${entry.path} digest mismatch`);
		}

		const recomputedTreeDigest = treeDigest(manifest.entries);
		if (recomputedTreeDigest !== manifest.tree.digest) throw new Error("assets tree digest mismatch");

		// whole-set TOCTOU re-check: the manifest plus every entry file, not
		// only the first and last file.
		const afterManifest = lstatSync(manifestPath);
		if (!sameFile(beforeManifest, afterManifest)) throw new Error("assets manifest replaced during verification");
		for (const { entryPath, before } of beforeEntries) {
			if (!sameFile(before, lstatSync(entryPath))) throw new Error("asset replaced during verification");
		}

		return { assetsDirectory, manifest };
	} catch {
		throw new PackageLocalGentleAiAssetsMissingError(assetsDirectory);
	}
}
