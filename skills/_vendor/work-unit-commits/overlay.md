# Overlay: work-unit-commits

No Pi-specific delta. Portability comparison found gentle-pi's checked-in
copy was missing content gentle-ai already had (the Work Unit Evidence
checklist items and the "implementation evidence MUST include" paragraph) --
content this repo already requires independently via `skills/sdd-apply`'s
Work Unit Evidence hard gate. Admission adopts the vendored body verbatim,
with zero anchor/replace blocks.
